---
all:
  children:
    demo_host:
      hosts:
        cert-demo:
          ansible_host: "{{ ec2_public_ip | default('127.0.0.1') }}"
          ansible_user: ec2-user
          ansible_ssh_private_key_file: "~/.ssh/{{ ec2_key_name }}.pem"
    web_servers:
      hosts:
        cert-demo:
    app_servers:
      hosts:
        cert-demo:

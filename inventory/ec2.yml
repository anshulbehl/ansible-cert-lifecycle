---
all:
  children:
    demo_host:
      hosts:
        cert-demo:
          ansible_host: 32.192.247.17
          ansible_user: ec2-user
          ansible_ssh_private_key_file: ~/.ssh/cert-lifecycle-demo-key.pem
    web_servers:
      hosts:
        cert-demo:
    app_servers:
      hosts:
        cert-demo:

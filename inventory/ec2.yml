---
all:
  children:
    demo_host:
      hosts:
        cert-demo:
          ansible_host: 44.220.167.79
          ansible_user: ec2-user
          ansible_ssh_private_key_file: ~/.ssh/cert-lifecycle-demo-key.pem
    web_servers:
      hosts:
        cert-demo:
    app_servers:
      hosts:
        cert-demo:
